// Declare a variable for the countdown start
let countdownStart = 5; 

// Countdown loop
for (var i = countdownStart; i >= 0; i--) {
    console.log(i);
}

// Display completion message
console.log("Countdown complete!");
